# 写作指南

这个仓库就是博客本身。写文章、加项目只需要按下面的约定放 Markdown 文件和素材，推送到 GitHub 后 Pages 会自动构建发布。

## 目录结构约定

```
_posts/                 文章，一本书 = 一个分类
_projects/              项目卡片和项目详情页
_templates/             写文章/项目时复制的模板（不会发布）
assets/posts/<slug>/    某篇文章的图片素材，一篇文章一个文件夹
assets/projects/        项目图标等素材
```

文章和项目的 `layout` 已在 `_config.yml` 里配置默认值，front matter 里**不用写 layout**。

## 写文章

1. 复制 `_templates/post.md` 到 `_posts/`，文件名格式必须是 `年-月-日-slug.md`，例如 `2026-09-08-i2c-bus-hang.md`。slug 用英文小写和连字符，它会出现在网址里。
2. 填写 front matter：

```yaml
---
title: "文章标题"
date: 2026-09-08 10:00:00 +0800
categories: 嵌入式 Linux        # 只写一个！它就是书架上的“那本书”，决定右侧“本书目录”
tags: [Linux, I2C]              # 可选
excerpt: "一句话摘要。"          # 推荐写，显示在标题下方和文章列表
# project: device-lab           # 可选，关联某个项目（填项目的 slug）
---
```

注意：
- `categories` **只能写一个分类名**。写多个（或用空格分隔）会被拆成好几本书，书架和“本书目录”就乱了。
- 标题层级用 `##`、`###`，至少两个标题左侧才会出现「本文目录」；不要用 `#` 一级标题。

### 书本（分类）的创建与删除

书不是单独建出来的，而是由文章的 `categories` 自动聚合：

- **创建一本书**：第一次在某篇文章的 `categories` 里写一个新分类名，书架上就会自动多出这本书。
- **删除一本书**：把属于这个分类的所有文章改成别的分类（或删除这些文章），书就自动消失。
- **归类**：文章的 `categories` 写哪本书，文章就属于哪本书，首页文章列表、书本目录、筛选按钮全部自动跟着变。
- **书的简介**：翻开书时左页显示的介绍文字，在 `index.html` 里搜 `{% case category %}`，照已有的格式加一段 `{% when "你的分类名" %}` 即可；不加就走默认文案。
- **书的封面颜色**：4 种颜色按书的位置自动轮换，不用管；想改配色就改 `assets/css/site.css` 里的 `.book-tone-1` 到 `.book-tone-4`。

### 文章里的图片

每篇文章的图片放进以自己的 slug 命名的文件夹：`assets/posts/i2c-bus-hang/波形.png`，正文里这样引用：

```markdown
![示波器波形](/assets/posts/i2c-bus-hang/波形.png)
```

## 加项目

1. 复制 `_templates/project.md` 到 `_projects/`，文件名就是项目 slug，例如 `sensor-hub.md`。
2. 填写 front matter：

```yaml
---
name: Sensor Hub              # 卡片上的小字英文名
title: 多路传感器采集网关       # 卡片主标题
stack: SPI / I2C / MQTT       # 技术栈
icon: cpu                      # 内置图标三选一：cpu / terminal / waveform
# image: /assets/projects/sensor-hub.svg   # 可选：自定义图标，优先于 icon
# link: https://github.com/...             # 可选：源码地址，详情页显示“查看源码”
---
```

注意：
- 自定义图标 **svg、png、jpg 都可以**，放到 `assets/projects/` 目录，用 `image` 指向它；不写 `image` 就用 `icon` 的内置图标。
- 项目详情页底部会自动列出 front matter 里写了 `project: sensor-hub` 的文章。
- 目前 `_projects/` 里 `demo-` 开头的三个是占位项目，替换成真实内容或直接删除即可。

## 数量与展示

- 文章数量没有上限，书架按分类（书）聚合。
- 项目卡片是手表式曲面滚动，数量没有上限，一次显示三张，上下滑动切换。
